=> installation of nodejs 
=> node
=> npm
=> import and export
=> express js
=> routes in express
=> dynamic routes in express
=> middleware in express
=> nodemon
=> template engine => {
    configure
    app.set('view engine','ejs');

    make views folder case sensitive 
    make ejs file in that folder
    and use render in place of send in routes
}
=> express-generator => {
    npm install express-generator -g
    to create a new project
    express app_name --view=ejs
    cd app_name   
    npm install
    npm start
    express-generator creates a basic structure of the project with some default files and folders for backend
}
=> install mongodb community server =>{
    install mongoose
    npm install mongoose
    require mongoose and setup connection
    
    create a schema
    create a model and export it
}
